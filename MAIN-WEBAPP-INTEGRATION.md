# 🏢 Main Webapp Integration Guide\n\n**Complete approval, execution, and order management system for your main application interface**\n\n---\n\n## 📋 Overview\n\nThe main webapp is your **complete control center** for managing all item requests from submission through payment processing. It handles:\n\n- ✅ Approval workflows (Building Tech → Tech Director)\n- ✅ Supplier quote management\n- ✅ Purchase order creation and tracking\n- ✅ Receipt and invoice management\n- ✅ Accounting handoff\n- ✅ Full audit trails and reporting\n\n---\n\n## 🏗️ Architecture\n\n### Location in Your App\n\n```\nMainApp.jsx or Dashboard.jsx (your existing dashboard)\n  │\n  ├─ Sidebar/Navigation\n  │  ├─ 📋 Requests\n  │  ├─ ✅ Approvals\n  │  ├─ 📦 Orders\n  │  ├─ 💰 Accounting\n  │  └─ 📊 Reports\n  │\n  └─ Content Area (role-based)\n     ├─ BuildingTechDashboard\n     ├─ MaintenanceOrgDashboard\n     ├─ TechDirectorDashboard\n     ├─ AccountantDashboard\n     └─ AdminDashboard\n```\n\n### NO Changes to Existing Code\n\n✅ **Your existing pages/components remain unchanged**\n- Users tab: Works as before\n- Spare parts: Works as before\n- All existing features: Works as before\n\n✅ **New features added in parallel**\n- New dashboard views (role-based)\n- New menu items (only if user has role)\n- New data queries (separate from existing)\n\n---\n\n## 🔐 Role-Based Dashboard System\n\n### How It Works\n\n```javascript\n// In MainApp.jsx or Dashboard.jsx\n\nconst MainApp = () => {\n  const user = useAuthContext(); // Your existing auth\n  const role = user.role; // operational_technician, building_tech, etc.\n\n  return (\n    <div className=\"main-app\">\n      {/* Your existing sidebar/navigation */}\n      <Sidebar />\n\n      {/* NEW: Request Management Section */}\n      {role === 'building_tech' && (\n        <div className=\"requests-section\">\n          <BuildingTechDashboard />\n        </div>\n      )}\n\n      {role === 'maintenance_org' && (\n        <div className=\"requests-section\">\n          <MaintenanceOrgDashboard />\n        </div>\n      )}\n\n      {role === 'tech_director' && (\n        <div className=\"requests-section\">\n          <TechDirectorDashboard />\n        </div>\n      )}\n\n      {role === 'god_admin' && (\n        <div className=\"requests-section\">\n          <AdminDashboard />\n        </div>\n      )}\n\n      {/* NEW ROLE: Accountant */}\n      {role === 'accountant' && (\n        <div className=\"requests-section\">\n          <AccountantDashboard />\n        </div>\n      )}\n\n      {/* Your existing content areas remain unchanged */}\n    </div>\n  );\n};\n```\n\n---\n\n## 🎯 Building Tech Dashboard\n\n### What They See\n\n```\nBUILDING TECH DASHBOARD\n├─ Pending Approvals (Level 1)\n│  ├─ List of submitted requests\n│  ├─ Filters: By Building, By Date, By Status\n│  └─ Quick actions: View, Approve, Reject\n│\n└─ My Approved Requests (Archive)\n   └─ Historical list with dates\n```\n\n### Key Component: RequestListView\n\n```jsx\n// src/components/main-app/BuildingTechDashboard.jsx\n\nconst BuildingTechDashboard = () => {\n  const [requests, setRequests] = useState([]);\n  const [selectedRequest, setSelectedRequest] = useState(null);\n  const { getPendingApprovals } = useRequestsApi();\n\n  useEffect(() => {\n    // Get requests waiting for Building Tech approval (Level 1)\n    getPendingApprovals('building_tech').then(setRequests);\n  }, []);\n\n  return (\n    <div className=\"dashboard\">\n      <h2>Pending Approvals (Level 1)</h2>\n      \n      {/* Request List */}\n      <div className=\"request-list\">\n        {requests.map(req => (\n          <div key={req.id} className=\"request-card\">\n            <div className=\"request-header\">\n              <span className=\"request-number\">{req.request_number}</span>\n              <span className=\"status-badge\">{req.status}</span>\n              <span className=\"date\">{new Date(req.created_at).toLocaleDateString()}</span>\n            </div>\n            \n            <div className=\"request-summary\">\n              <p><strong>Building:</strong> {req.building}</p>\n              <p><strong>Priority:</strong> {req.priority}</p>\n              <p><strong>Budget:</strong> ${req.total_amount}</p>\n              <p><strong>Items:</strong> {req.items.length}</p>\n            </div>\n            \n            <div className=\"request-actions\">\n              <button onClick={() => setSelectedRequest(req)}>\n                View Details\n              </button>\n            </div>\n          </div>\n        ))}\n      </div>\n\n      {/* Request Details Modal */}\n      {selectedRequest && (\n        <RequestDetailsModal\n          request={selectedRequest}\n          onClose={() => setSelectedRequest(null)}\n          showApprovalPanel={true}\n          userRole=\"building_tech\"\n        />\n      )}\n    </div>\n  );\n};\n```\n\n### Key Component: RequestApprovalPanel\n\n```jsx\n// In RequestDetailsModal or as separate panel\n\nconst RequestApprovalPanel = ({\n  request,\n  userRole,\n  onApprove,\n  onReject\n}) => {\n  const [comment, setComment] = useState('');\n  const { approveRequest, rejectRequest } = useRequestsApi();\n\n  const handleApprove = async () => {\n    await approveRequest(request.id, {\n      approval_level: getApprovalLevel(userRole),\n      approval_role: userRole,\n      approval_comments: comment\n    });\n    onApprove();\n  };\n\n  const handleReject = async () => {\n    await rejectRequest(request.id, {\n      reason: comment,\n      rejection_level: getApprovalLevel(userRole),\n      rejection_role: userRole\n    });\n    onReject();\n  };\n\n  return (\n    <div className=\"approval-panel\">\n      <h3>Approval Decision</h3>\n      \n      <div className=\"form-group\">\n        <label>Comments (Optional)</label>\n        <textarea\n          value={comment}\n          onChange={(e) => setComment(e.target.value)}\n          placeholder=\"Add any comments or notes...\"\n        />\n      </div>\n\n      <div className=\"approval-actions\">\n        <button className=\"btn btn-success\" onClick={handleApprove}>\n          ✅ Approve & Move to Next Level\n        </button>\n        <button className=\"btn btn-danger\" onClick={handleReject}>\n          ❌ Reject & Return to Submitter\n        </button>\n      </div>\n    </div>\n  );\n};\n```\n\n---\n\n## 📦 Maintenance Organizer Dashboard\n\n### What They See\n\n```\nMAINTENANCE ORGANIZER DASHBOARD\n├─ Pending Quote Requests\n│  ├─ Requests ready for supplier quote\n│  ├─ \"Create Quote Request\" button\n│  └─ Track quote status\n│\n├─ Active Orders\n│  ├─ POs sent to suppliers\n│  ├─ Track delivery status\n│  ├─ Update tracking numbers\n│  └─ Mark items as received\n│\n├─ Items Received\n│  ├─ Verify items against PO\n│  ├─ Upload invoice checklist\n│  └─ Track documentation\n│\n└─ Ready for Accounting\n   ├─ All documents complete\n   └─ \"Send to Accounting\" button\n```\n\n### Key Component: QuoteManagementPanel\n\n```jsx\n// src/components/main-app/QuoteManagementPanel.jsx\n\nconst QuoteManagementPanel = ({ request }) => {\n  const [quote, setQuote] = useState(null);\n  const [prices, setPrices] = useState({});\n  const [quoteFile, setQuoteFile] = useState(null);\n  const { createQuoteRequest, processQuote } = useQuotesApi();\n\n  const handleCreateQuoteRequest = async () => {\n    // Send to supplier (via email or portal)\n    await createQuoteRequest(request.id);\n    // Status: QUOTE_REQUESTED\n  };\n\n  const handlePriceUpdate = (itemId, price) => {\n    setPrices(prev => ({\n      ...prev,\n      [itemId]: price\n    }));\n  };\n\n  const handleProcessQuote = async () => {\n    await processQuote(request.id, {\n      item_prices: prices,\n      quote_pdf_url: quoteFile.url,\n      supplier_contact: quote.supplier_contact\n    });\n    // Status: QUOTE_PROCESSED\n  };\n\n  return (\n    <div className=\"quote-panel\">\n      {!quote ? (\n        <button onClick={handleCreateQuoteRequest}>\n          📨 Create Quote Request to Supplier\n        </button>\n      ) : (\n        <div>\n          <h3>Supplier Quote Received</h3>\n          \n          {/* Items with pricing */}\n          <table>\n            <thead>\n              <tr>\n                <th>Item</th>\n                <th>Qty</th>\n                <th>Supplier Price</th>\n                <th>Our Price</th>\n              </tr>\n            </thead>\n            <tbody>\n              {request.items.map(item => (\n                <tr key={item.id}>\n                  <td>{item.description}</td>\n                  <td>{item.quantity}</td>\n                  <td>${quote.item_prices?.[item.id] || 0}</td>\n                  <td>\n                    <input\n                      type=\"number\"\n                      value={prices[item.id] || ''}\n                      onChange={(e) => handlePriceUpdate(item.id, e.target.value)}\n                      placeholder=\"Enter final price\"\n                    />\n                  </td>\n                </tr>\n              ))}\n            </tbody>\n          </table>\n\n          {/* Quote PDF Upload */}\n          <div className=\"form-group\">\n            <label>Upload Quote PDF</label>\n            <input\n              type=\"file\"\n              accept=\".pdf\"\n              onChange={(e) => setQuoteFile(e.target.files[0])}\n            />\n          </div>\n\n          <button onClick={handleProcessQuote}>\n            ✅ Process Quote & Send for Approval\n          </button>\n        </div>\n      )}\n    </div>\n  );\n};\n```\n\n### Key Component: OrderTrackingPanel\n\n```jsx\n// src/components/main-app/OrderTrackingPanel.jsx\n\nconst OrderTrackingPanel = ({ request }) => {\n  const [order, setOrder] = useState(null);\n  const [trackingNumber, setTrackingNumber] = useState('');\n  const { placeOrder, updateOrderStatus, markItemsReceived } = useOrdersApi();\n\n  const handlePlaceOrder = async () => {\n    await placeOrder(request.id);\n    // Status: ORDER_PLACED\n  };\n\n  const handleUpdateTracking = async () => {\n    await updateOrderStatus(request.id, {\n      tracking_number: trackingNumber,\n      status: 'IN_TRANSIT',\n      expected_delivery: calculateDeliveryDate(trackingNumber)\n    });\n  };\n\n  const handleMarkReceived = async () => {\n    await markItemsReceived(request.id, {\n      received_date: new Date(),\n      verified_by: currentUser.id\n    });\n    // Status: ITEMS_RECEIVED\n    // Invoice Checklist appears\n  };\n\n  return (\n    <div className=\"order-panel\">\n      {!order && (\n        <button onClick={handlePlaceOrder}>\n          📋 Place Purchase Order\n        </button>\n      )}\n\n      {order?.status === 'ORDER_PLACED' && (\n        <div>\n          <h3>Awaiting Supplier Confirmation</h3>\n          <p>Last checked: {order.last_updated}</p>\n        </div>\n      )}\n\n      {order?.status === 'ORDER_CONFIRMED' && (\n        <div>\n          <h3>Order Confirmed - In Preparation</h3>\n          <div className=\"form-group\">\n            <label>Update when shipped:</label>\n            <input\n              type=\"text\"\n              value={trackingNumber}\n              onChange={(e) => setTrackingNumber(e.target.value)}\n              placeholder=\"Enter tracking number\"\n            />\n            <button onClick={handleUpdateTracking}>Update Status</button>\n          </div>\n        </div>\n      )}\n\n      {order?.status === 'IN_TRANSIT' && (\n        <div>\n          <h3>📦 Package in Transit</h3>\n          <p>Tracking: {order.tracking_number}</p>\n          <p>Expected delivery: {order.expected_delivery}</p>\n          <button onClick={handleMarkReceived}>\n            ✅ Mark Items as Received\n          </button>\n        </div>\n      )}\n\n      {order?.status === 'RECEIVED' && (\n        <InvoiceChecklistWidget request={request} />\n      )}\n    </div>\n  );\n};\n```\n\n### Key Component: InvoiceChecklistWidget\n\n```jsx\n// src/components/main-app/InvoiceChecklistWidget.jsx\n\nconst InvoiceChecklistWidget = ({ request }) => {\n  const [checklist, setChecklist] = useState({\n    invoice_received: false,\n    transport_docs_received: false,\n    proforma_invoice: request.has_proforma || false,\n    final_invoice: false\n  });\n\n  const [documents, setDocuments] = useState({});\n\n  const handleChecklistItem = (item, value) => {\n    setChecklist(prev => ({\n      ...prev,\n      [item]: value\n    }));\n  };\n\n  const handleDocumentUpload = async (item, file) => {\n    // Upload to storage\n    const url = await uploadDocument(file);\n    setDocuments(prev => ({\n      ...prev,\n      [item]: url\n    }));\n  };\n\n  const allComplete = Object.values(checklist).every(v => v);\n\n  return (\n    <div className=\"invoice-checklist\">\n      <h3>📋 Invoice & Documentation Checklist</h3>\n\n      <div className=\"checklist-item\">\n        <label>\n          <input\n            type=\"checkbox\"\n            checked={checklist.invoice_received}\n            onChange={(e) => handleChecklistItem('invoice_received', e.target.checked)}\n          />\n          Invoice Received\n        </label>\n        {checklist.invoice_received && !documents.invoice && (\n          <input\n            type=\"file\"\n            onChange={(e) => handleDocumentUpload('invoice', e.target.files[0])}\n          />\n        )}\n      </div>\n\n      <div className=\"checklist-item\">\n        <label>\n          <input\n            type=\"checkbox\"\n            checked={checklist.transport_docs_received}\n            onChange={(e) => handleChecklistItem('transport_docs_received', e.target.checked)}\n          />\n          Transportation Documents Received\n        </label>\n        {checklist.transport_docs_received && !documents.transport && (\n          <input\n            type=\"file\"\n            onChange={(e) => handleDocumentUpload('transport', e.target.files[0])}\n          />\n        )}\n      </div>\n\n      {checklist.proforma_invoice && (\n        <div className=\"checklist-item\">\n          <label>\n            <input\n              type=\"checkbox\"\n              checked={checklist.proforma_invoice}\n              onChange={(e) => handleChecklistItem('proforma_invoice', e.target.checked)}\n            />\n            Proforma Invoice Received (Advance Payment)\n          </label>\n          {checklist.proforma_invoice && !documents.proforma && (\n            <input\n              type=\"file\"\n              onChange={(e) => handleDocumentUpload('proforma', e.target.files[0])}\n            />\n          )}\n        </div>\n      )}\n\n      <div className=\"checklist-item\">\n        <label>\n          <input\n            type=\"checkbox\"\n            checked={checklist.final_invoice}\n            onChange={(e) => handleChecklistItem('final_invoice', e.target.checked)}\n          />\n          Final Invoice Received\n        </label>\n        {checklist.final_invoice && !documents.final_invoice && (\n          <input\n            type=\"file\"\n            onChange={(e) => handleDocumentUpload('final_invoice', e.target.files[0])}\n          />\n        )}\n      </div>\n\n      {allComplete && (\n        <div className=\"success-message\">\n          ✅ All documentation complete! Ready to send to accounting.\n          <button onClick={() => submitToAccounting(request.id)}>\n            📤 Send to Accounting\n          </button>\n        </div>\n      )}\n    </div>\n  );\n};\n```\n\n---\n\n## 💰 Tech Director Dashboard\n\n### What They See\n\n```\nTECH DIRECTOR DASHBOARD\n├─ Pending Approvals (Level 3)\n│  ├─ Requests with quotes attached\n│  ├─ Review prices\n│  └─ Approve or Reject\n│\n└─ Approved Requests (Archive)\n   └─ Historical decisions\n```\n\n### Implementation\n\n```jsx\n// src/components/main-app/TechDirectorDashboard.jsx\n\nconst TechDirectorDashboard = () => {\n  const [requests, setRequests] = useState([]);\n  const [selectedRequest, setSelectedRequest] = useState(null);\n\n  useEffect(() => {\n    // Get requests with quotes, ready for Tech Director approval\n    getPendingApprovals('tech_director').then(setRequests);\n  }, []);\n\n  return (\n    <div className=\"dashboard\">\n      <h2>Pending Approvals (Level 3)</h2>\n      <p>Tech Director review of requested items and supplier quotes</p>\n\n      <div className=\"request-list\">\n        {requests.map(req => (\n          <div key={req.id} className=\"request-card\">\n            <div className=\"request-header\">\n              <span>{req.request_number}</span>\n              <span className=\"status\">QUOTE_PROCESSED</span>\n            </div>\n\n            <div className=\"quote-summary\">\n              <p>📌 Quote Price: <strong>${req.quote?.total || 0}</strong></p>\n              <p>🏢 Building: {req.building}</p>\n              <p>⚡ Priority: {req.priority}</p>\n            </div>\n\n            <button onClick={() => setSelectedRequest(req)}>\n              View with Quote\n            </button>\n          </div>\n        ))}\n      </div>\n\n      {selectedRequest && (\n        <RequestDetailsModal\n          request={selectedRequest}\n          showQuoteDetails={true}\n          showApprovalPanel={true}\n          userRole=\"tech_director\"\n          onClose={() => setSelectedRequest(null)}\n        />\n      )}\n    </div>\n  );\n};\n```\n\n---\n\n## 💼 Accountant Dashboard (NEW ROLE)\n\n### What They See\n\n```\nACCOUNTANT DASHBOARD\n├─ Pending Payments\n│  ├─ Requests with all documents attached\n│  ├─ View all invoices\n│  └─ Process payment\n│\n├─ Payment History\n│  └─ Completed transactions\n│\n└─ Financial Reports\n   ├─ Total spent vs budget\n   └─ Supplier expenses\n```\n\n### Implementation\n\n```jsx\n// src/components/main-app/AccountantDashboard.jsx\n\nconst AccountantDashboard = () => {\n  const [pendingPayments, setPendingPayments] = useState([]);\n\n  useEffect(() => {\n    // Get all requests ready for payment (DOCUMENTATION_COMPLETE)\n    getPendingPayments().then(setPendingPayments);\n  }, []);\n\n  return (\n    <div className=\"dashboard\">\n      <h2>💰 Pending Payments</h2>\n\n      <div className=\"payment-list\">\n        {pendingPayments.map(req => (\n          <div key={req.id} className=\"payment-card\">\n            <div className=\"payment-header\">\n              <span>{req.request_number}</span>\n              <span className=\"amount\">${req.total_amount}</span>\n            </div>\n\n            <div className=\"payment-details\">\n              <p>Supplier: {req.supplier_name}</p>\n              <p>Invoice: {req.invoice_number}</p>\n              <p>Due Date: {req.due_date}</p>\n            </div>\n\n            <div className=\"documents\">\n              <h4>📎 Attached Documents:</h4>\n              <ul>\n                {req.documents.map(doc => (\n                  <li key={doc.id}>\n                    📄 {doc.type}: <a href={doc.url}>View</a>\n                  </li>\n                ))}\n              </ul>\n            </div>\n\n            <button className=\"btn btn-success\">\n              ✅ Process Payment\n            </button>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};\n```\n\n---\n\n## 🔧 Admin Dashboard\n\n### What They See\n\n```\nADMIN DASHBOARD\n├─ All Requests (Any Status)\n├─ Override Any Approval\n├─ Cancel Orders\n├─ Edit Requests\n├─ Full Audit Trail\n└─ System Administration\n```\n\n---\n\n## 📊 API Integration\n\n### Getting Pending Approvals\n\n```javascript\n// src/hooks/useRequestsApi.js\n\nconst useRequestsApi = () => {\n  const getPendingApprovals = async (role) => {\n    const response = await fetch('/api/requests/pending-approvals', {\n      headers: { \n        Authorization: `Bearer ${token}`,\n        'X-User-Role': role\n      }\n    });\n    return response.json();\n  };\n\n  const approveRequest = async (requestId, approvalData) => {\n    const response = await fetch(`/api/requests/${requestId}/approve`, {\n      method: 'POST',\n      headers: { Authorization: `Bearer ${token}` },\n      body: JSON.stringify(approvalData)\n    });\n    return response.json();\n  };\n\n  return { getPendingApprovals, approveRequest, ... };\n};\n```\n\n### Using in Components\n\n```javascript\nconst BuildingTechDashboard = () => {\n  const { getPendingApprovals } = useRequestsApi();\n\n  useEffect(() => {\n    getPendingApprovals('building_tech').then(data => {\n      console.log('Building Tech pending requests:', data);\n    });\n  }, []);\n};\n```\n\n---\n\n## 🚀 Implementation Steps\n\n### Step 1: Create Dashboard Structure (2 hours)\n```\n1. Create src/pages/MainApp/ directory\n2. Create 5 dashboard files:\n   - BuildingTechDashboard.jsx\n   - MaintenanceOrgDashboard.jsx\n   - TechDirectorDashboard.jsx\n   - AccountantDashboard.jsx\n   - AdminDashboard.jsx\n3. Create routing logic in MainApp.jsx\n```\n\n### Step 2: Create Shared Components (2 hours)\n```\n1. RequestDetailsModal.jsx - Full view with all sections\n2. RequestApprovalPanel.jsx - Approve/Reject interface\n3. QuoteManagementPanel.jsx - Quote handling\n4. OrderTrackingPanel.jsx - Order status\n5. InvoiceChecklistWidget.jsx - Documentation tracking\n```\n\n### Step 3: Create API Hooks (1 hour)\n```\n1. Update useRequestsApi.js with new methods:\n   - getPendingApprovals(role)\n   - approveRequest(id, data)\n   - rejectRequest(id, data)\n   - editRequest(id, data)\n2. Create useQuotesApi.js with:\n   - createQuoteRequest(id)\n   - processQuote(id, data)\n3. Create useOrdersApi.js with:\n   - placeOrder(id)\n   - updateOrderStatus(id, data)\n   - markItemsReceived(id, data)\n```\n\n### Step 4: Create Backend API Endpoints (2 hours)\n```\n1. Update src/api/requests.js with new endpoints:\n   - GET /api/requests/pending-approvals (filtered by role)\n   - POST /api/requests/:id/approve\n   - POST /api/requests/:id/reject\n2. Create src/api/quotes.js\n3. Create src/api/orders.js\n```\n\n### Step 5: Create Database Migration (1 hour)\n```\n1. Create src/database/migrations/002-supplier-quotes.sql\n2. Add supplier_quotes table\n3. Add order_tracking table\n4. Add invoice_checklist table\n5. Add new indexes and RLS policies\n```\n\n### Step 6: Integrate with Existing App (1 hour)\n```\n1. Add new section to MainApp.jsx\n2. Add sidebar menu items (conditional by role)\n3. Link from technician login to main webapp\n4. Test end-to-end workflow\n```\n\n**Total: ~9 hours of implementation**\n\n---\n\n## ✅ Integration Checklist\n\n- [ ] Dashboard components created\n- [ ] Approval panels working\n- [ ] Quote management UI complete\n- [ ] Order tracking UI complete\n- [ ] Invoice checklist complete\n- [ ] All APIs integrated\n- [ ] Database migrations applied\n- [ ] Role-based routing working\n- [ ] Sidebar menu items showing correctly\n- [ ] Links from technician login work\n- [ ] Approval workflow tested\n- [ ] Quote workflow tested\n- [ ] Order workflow tested\n- [ ] Accounting handoff tested\n- [ ] Audit trail complete\n- [ ] No existing features broken\n\n---\n\n## 🎯 Key Design Principles\n\n✅ **Separation of Concerns**\n- Technician login: Create & view status only\n- Main webapp: Manage & approve only\n- Clean boundary between interfaces\n\n✅ **Role-Based Access**\n- Each dashboard only shows relevant data\n- Automatic filtering by user role\n- No manual permission checks needed\n\n✅ **Zero Breaking Changes**\n- All existing code untouched\n- New features in parallel\n- Can disable by feature flag if needed\n\n✅ **Complete Audit Trail**\n- Every action logged\n- Who, what, when, why\n- Traceable to payment\n\n---\n\n**Ready to Build!** 🚀\n\n*Main Webapp Integration Guide*\n*Complete approval, execution, and order management system*\n*January 10, 2026*", "sha": ""}